
<%= link_to("<< Back to List", {:action => 'list'}, :class => 'back-link') %>


  <h2>Delete Flat Holder</h2>
  
  <%= form_for(:create_owner, :url => {:action => 'destroy', :id => @create_owner.id}) do |f| -%>

    <h1>Are you sure you want to permanently delete this subject?</h1>
  
    <p class="reference-name"><%= @create_owner.name %></p>
  
    <%= f.submit 'Delete Owner', :class =>'form-buttons' %>
   
    
  <% end %>

